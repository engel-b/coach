# WebSocket-Schnittstellen

Die WebSocket-Verträge sind in der Projektseite dokumentiert.

**Aktuelle Fassung:** [WebSocket-Schnittstellen](../../docs/schnittstellen/websockets.md).

Lokal anzeigen: `cd docs && npm ci && npm run dev`.
